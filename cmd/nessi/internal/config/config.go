package config

// Implement configuration management for Nessi.dev
// The configuration should include:
// - Server settings (host, port, TLS)
// - Extension settings (enabled extensions, Python settings if available)
// - Delta Lake settings (default paths, etc.)
// - Monitoring settings (metrics, alerts)
// - Security settings (authentication, authorization)
// The config should be loadable from a YAML file and environment variables
// Environment variables should override file settings
