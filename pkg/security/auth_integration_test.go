package security_test

import (
	"testing"
)

func TestAuthIntegration(t *testing.T) {
	// Skip tests for CLI-only approach
	t.Skip("Skipping auth integration tests in CLI-only mode")
}

func TestAuthManagerPersistence(t *testing.T) {
	// Skip tests for CLI-only approach
	t.Skip("Skipping auth persistence tests in CLI-only mode")
}

func TestRequireHTTPS(t *testing.T) {
	// Skip tests for CLI-only approach
	t.Skip("Skipping HTTPS requirement tests in CLI-only mode")
}
