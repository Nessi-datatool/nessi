package monitoring

// This file contains stub implementations for CLI-only compatibility
// The original web handlers are no longer needed in the CLI-only approach

// Empty file for CLI-only compatibility
